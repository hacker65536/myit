
show other (i.e. untracked) files in the output
```
git ls-files -o
```


show conflict (

```
git ls-files -u <path>
```


show cached files(index)
```
git ls-files -c
```

show modified
```
git ls-files -m
```

show stage
```
git ls-files -s
```
